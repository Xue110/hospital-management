// 统一中转工具模块函数
// import { request } from "@/utils"
import request from "./request"
import { getToken,setToken,clearToken } from "./token"
export{
    request,
    getToken,
    setToken,
    clearToken
}